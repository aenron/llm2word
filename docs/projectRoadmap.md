## 目标一：构建可调用的 DOCX 生成服务
- [x] 搭建 FastAPI 服务入口与健康检查
- [x] 提供接收外部 JSON 的生成接口
- [x] 返回标准 DOCX 文件流下载响应

## 目标二：实现会议议程模板化渲染
- [x] 建立 `docxtpl` 渲染链路
- [x] 增加模板自动引导创建能力
- [x] 支持标题、元信息、议程列表写入

## 目标三：可控样式能力
- [x] 支持标题字号/加粗/字体设置
- [x] 支持正文行距与缩进层级设置
- [x] 支持行首前缀加粗

## 目标四：工程化与可维护性
- [x] 规范化 Pydantic 数据模型
- [x] 按职责拆分渲染与模板模块
- [x] 增加 MCP SSE 工具封装并复用核心生成逻辑
- [ ] 增加集成测试与示例回归测试

## 完成标准
- [x] 外部系统以 JSON 调用接口可获得 docx 文件
- [x] MCP 客户端可通过 SSE 工具调用获得一致结果
- [x] 代码结构清晰，便于扩展更多模板
- [ ] 与目标视觉样式进行逐项像素级校验

## 已完成任务
- [x] 初始化 `app/main.py` FastAPI 服务
- [x] 新增 `app/schemas.py` 请求与样式模型
- [x] 新增 `app/template_builder.py` 模板引导创建
- [x] 新增 `app/render_service.py` 文档渲染服务
- [x] 增加依赖清单 `requirements.txt`
- [x] 新增 `app/mcp_sse_server.py` 提供 SSE 协议 MCP 工具
